PORT=5000
NODE_ENV=development

# MongoDB Atlas (or local) connection string — added in Phase 10
MONGODB_URI=

# Secret used to sign JWTs — added in Phase 11
JWT_SECRET=

# AI provider key (e.g. Anthropic, OpenAI) — added in Phase 6
AI_API_KEY=
AI_PROVIDER=

# Web search provider key — added in Phase 5
SEARCH_API_KEY=
SEARCH_PROVIDER=

# Frontend origin, used for CORS
CLIENT_URL=http://localhost:5173
